<%= form_for(setup_pdk(@pdk)) do |f| %>
  <% if @pdk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdk.errors.count, "error") %> prohibited this pdk from being saved:</h2>

      <ul>
      <% @pdk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <hr>
      Layers!
    <hr>
      <!-- <%= f.fields_for :pdk_layers do |ff| %>
        <%= ff.label     :pdk_id, ff.object.layer.name %>
        <%= ff.check_box :pdk_id %> 
      <% end %> -->

<hr>
  <%= f.collection_check_boxes :layer_ids, Layer.all, :id, :name %>
    <hr>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
