<% title = (params[:action]+' '+params[:controller].singularize).split.map(&:capitalize).join(' ') %>
<% provide(:title, title) %>
<% provide(:h, title) %>

<%= render 'form' %>

<%= link_to 'Back', colors_path %>
