<%= form_for(@stipple) do |f| %>
  <% if @stipple.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stipple.errors.count, "error") %> prohibited this stipple from being saved:</h2>

      <ul>
      <% @stipple.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <table id="stipple_table" border=1 cellpadding=15 cellspacing=0>
      <%= render 'pattern' %>
    </table>
    <%= f.text_field :pattern, hidden: true %>
  </div>
  <div class="actions">
    <%= f.submit id: "commit" %>
  </div>
<% end %>
